#ifndef __UTILS__
#define __UTILS__

namespace utils {

    /*enum Direction {
        UP, 
        DOWN, 
        LEFT, 
        RIGHT, 
        STAY
    };*/

    enum BombState {
        INERT,
        EXPLOSING
    };
}

#endif